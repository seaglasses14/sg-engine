{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run/Debug OpenGL",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${command:cmake.launchTargetDirectory}",
            "console": "integratedTerminal",
            "stopAtEntry": false
        }
    ]
}